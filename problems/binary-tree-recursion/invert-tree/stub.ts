export interface TreeNode {
  val: number
  left: TreeNode | null
  right: TreeNode | null
}

/**
 * Mirrors the tree by swapping every node's children. Modifying the nodes in
 * place is allowed.
 *
 * @param root the root of a binary tree, or null for an empty tree.
 * @returns the root of the mirrored tree.
 */
export function invertTree(root: TreeNode | null): TreeNode | null {
  throw new Error('not implemented')
}
