/**
 * @param grid an n x n binary matrix; 0 is clear, 1 is blocked.
 * @returns the number of cells on the shortest clear path from the
 *   top-left to the bottom-right cell, moving in any of 8 directions and
 *   passing only through 0 cells, or -1 if no such path exists.
 */
export function shortestClearPath(grid: number[][]): number {
  throw new Error('not implemented')
}
