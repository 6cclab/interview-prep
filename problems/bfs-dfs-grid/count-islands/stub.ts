/**
 * @param grid an m x n matrix whose cells are the strings '1' (land) and
 *   '0' (water). May be modified in place.
 * @returns the number of islands — maximal groups of land cells connected
 *   to each other horizontally or vertically. Cells touching only at a
 *   corner are not connected.
 */
export function countIslands(grid: string[][]): number {
  throw new Error('not implemented')
}
