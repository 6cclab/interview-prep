/**
 * @param piles pile sizes, in bananas
 * @param h     hours available before the guards return
 * @returns the minimum integer eating speed that finishes all piles in time
 */
export function minEatingSpeed(piles: number[], h: number): number {
  throw new Error('not implemented')
}
