/**
 * @param edges edges[i] = [a, b] is an undirected connection between nodes a
 *              and b. The full list describes a tree on 1..n plus one extra
 *              edge, in the order the edges were added.
 * @returns the edge to delete so the graph is a tree again, oriented exactly
 *          as it appeared in `edges`. If more than one edge would work, the
 *          one that appears last in `edges` wins.
 */
export function findRedundantConnection(edges: [number, number][]): [number, number] {
  throw new Error('not implemented')
}
