/**
 * @param n the board size — an n x n board, and n queens to place on it.
 * @returns the number of distinct ways to place n queens on the board so
 *   that no two attack each other.
 */
export function countNQueens(n: number): number {
  throw new Error('not implemented')
}
