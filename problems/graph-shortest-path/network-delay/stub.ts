/**
 * @param times times[i] = [u, v, w]: a signal sent from node u reaches node v
 *              after w time units. Edges are directed: u -> v, not v -> u.
 * @param n     number of nodes, labelled 1..n
 * @param k     the node the signal starts from
 * @returns the time for every node to receive the signal, or -1 if some node
 *          never does
 */
export function networkDelayTime(times: number[][], n: number, k: number): number {
  throw new Error('not implemented')
}
