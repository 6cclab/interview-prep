export interface ListNode {
  val: number
  next: ListNode | null
}

/**
 * @param a head of a list sorted ascending, or null. Relinking its nodes is allowed.
 * @param b head of a list sorted ascending, or null. Relinking its nodes is allowed.
 * @returns the head of one sorted list containing every node from both inputs.
 */
export function mergeTwoSorted(a: ListNode | null, b: ListNode | null): ListNode | null {
  throw new Error('not implemented')
}
