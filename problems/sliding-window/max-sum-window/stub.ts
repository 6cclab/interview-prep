/**
 * @param values the array to scan.
 * @param k the window size, in number of consecutive values.
 * @returns the largest sum of any k consecutive values, or null if k exceeds
 *   the length of the array.
 */
export function maxWindowSum(values: number[], k: number): number | null {
  throw new Error('not implemented')
}
