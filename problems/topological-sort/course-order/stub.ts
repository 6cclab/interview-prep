/**
 * @param numCourses    courses are labelled 0..numCourses-1
 * @param prerequisites prerequisites[i] = [a, b] means course b must be
 *                       taken before course a (e.g. [1, 0] means 0 before 1)
 * @returns any order in which all numCourses courses can be taken while
 *          respecting every prerequisite, or an empty array if no such
 *          order exists
 */
export function findOrder(
  numCourses: number,
  prerequisites: number[][],
): number[] {
  throw new Error('not implemented')
}
