/**
 * @param s lowercase English letters only, possibly empty.
 * @returns the index of the first character occurring exactly once in `s`, or
 *   -1 if there is no such character.
 */
export function firstUniqChar(s: string): number {
  throw new Error('not implemented')
}
