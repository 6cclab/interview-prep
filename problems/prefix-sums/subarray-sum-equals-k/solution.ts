/**
 * @param nums the array. Values may be negative.
 * @param k    the target sum.
 * @returns the number of contiguous subarrays of `nums` that sum to exactly `k`.
 */
export function subarraySum(nums: number[], k: number): number {
  throw new Error('not implemented')
}
