/**
 * @param values the array to query. It does not change between queries.
 * @param queries pairs of [from, to], inclusive at both ends.
 * @returns one sum per query, in the order the queries were given.
 */
export function rangeSums(values: number[], queries: [number, number][]): number[] {
  throw new Error('not implemented')
}
