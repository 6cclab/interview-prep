/**
 * @param coins available coin denominations, unlimited supply of each.
 * @param amount the target sum.
 * @returns the fewest coins that sum to amount, or -1 if it's unreachable.
 */
export function coinChange(coins: number[], amount: number): number {
  throw new Error('not implemented')
}
