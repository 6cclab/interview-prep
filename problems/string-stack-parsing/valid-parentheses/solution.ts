/**
 * @param text a string of only the characters ( ) [ ] { }.
 * @returns true if every bracket is closed by its matching kind, in order.
 */
export function isBalanced(text: string): boolean {
  throw new Error('not implemented')
}
