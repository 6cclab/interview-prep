/**
 * Reverses `chars` in place. The caller reads the array it passed in, so the
 * mutation is the result — the return value is ignored.
 *
 * @param chars single characters. Modified in place.
 */
export function reverseString(chars: string[]): void {
  throw new Error('not implemented')
}
