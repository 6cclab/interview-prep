/**
 * @param n   number of items, labelled by index 0..n-1
 * @param get get(i) returns the label at index i. Calling it is not free.
 * @param set set(i, v) overwrites the label at index i. Calling it is not free.
 */
export function sortColors(
  n: number,
  get: (i: number) => number,
  set: (i: number, v: number) => void,
): void {
  throw new Error('not implemented')
}
