/**
 * Moves every zero in `nums` to the end, preserving the relative order of the
 * non-zero values. Modifies the array in place; the return value is ignored.
 *
 * @param nums the values. Modified in place.
 */
export function moveZeroes(nums: number[]): void {}
