export type Interval = [number, number]

/**
 * @param intervals a list of [start, end] pairs, in no particular order.
 *   Must not be mutated.
 * @returns the merged intervals, sorted by start, with no overlaps or
 *   touching intervals remaining.
 */
export function mergeIntervals(intervals: Interval[]): Interval[] {
  throw new Error('not implemented')
}
