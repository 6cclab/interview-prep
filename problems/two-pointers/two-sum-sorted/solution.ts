/**
 * @param numbers sorted non-decreasing.
 * @param target the sum to hit.
 * @returns the two positions as [i, j] with i < j, or null if there is no pair.
 */
export function twoSum(numbers: number[], target: number): [number, number] | null {
  throw new Error('not implemented')
}
