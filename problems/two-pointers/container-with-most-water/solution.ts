/**
 * @param heights heights[i] is the height of the vertical line at index i.
 * @returns the maximum area enclosed by any two lines and the x-axis.
 */
export function maxArea(heights: number[]): number {
  throw new Error('not implemented')
}
