export interface ListNode {
  val: number
  next: ListNode | null
}

/**
 * @param head head of a singly linked list, or null. Must not be modified.
 * @returns the middle node — the second of the two middles when the length is
 *   even — or null for an empty list.
 */
export function middleNode(head: ListNode | null): ListNode | null {
  throw new Error('not implemented')
}
