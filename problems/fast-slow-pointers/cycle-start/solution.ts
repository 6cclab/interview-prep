export interface ListNode {
  val: number
  next: ListNode | null
}

/**
 * @param head head of a singly linked list. Must not be mutated.
 * @returns the node where the cycle begins, or null if there is no cycle.
 */
export function detectCycleStart(head: ListNode | null): ListNode | null {
  throw new Error('not implemented')
}
